import { StyleSheet } from "react-native";
import { colors } from "../../../commons/styles";

export const headerStyle = StyleSheet.create<any>({
  headerTintColor: colors.razorPurple,
  headerStyle: {
    backgroundColor: colors.black,
  }
});